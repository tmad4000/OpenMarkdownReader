<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Application identifier (must match provisioning profile) -->
    <key>com.apple.application-identifier</key>
    <string>JESMXK96LG.com.jacobcole.openmarkdownreader</string>

    <!-- Team identifier -->
    <key>com.apple.developer.team-identifier</key>
    <string>JESMXK96LG</string>

    <!-- Required for Mac App Store -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- Required for Electron/V8 JIT compilation -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow read/write access to user-selected files (via Open/Save dialogs) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Allow access to user's Downloads folder for saving exports -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- Network access for any future features (optional, remove if not needed) -->
    <key>com.apple.security.network.client</key>
    <true/>
</dict>
</plist>
